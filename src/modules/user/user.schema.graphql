type User {
  id: ID!
  name: String!
  email: String!
}
type UserError {
  message: String!
  code: String!
}
type Query {
  usersAll: [User!]!
  userById(id: ID!): UserResult
}


union UserResult = User | UserError
